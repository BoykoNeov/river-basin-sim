# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Project data & results (large, regenerated) — HANDOFF §6
/data/*
!/data/.gitkeep
*.zarr/
results/
frames/
status.json

# Godot 4 viewer caches
viewer/.godot/
viewer/.import/
viewer/uid_cache.bin

# Terrain3D addon: ~50 MB of third-party GDExtension binaries, fetched not vendored.
# Install with viewer/scripts/fetch_terrain3d.sh (or Godot AssetLib). See viewer/README.
viewer/addons/

# Third-party Godot editor tooling dropped at the repo root (e.g. the gdai MCP
# plugin): ~16 MB of GDExtension binaries, and there is no project.godot here
# anyway, so Godot never loads it from this path. Not vendored.
/addons/

# Throwaway Terrain3D compatibility-probe project
.t3dtest/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
